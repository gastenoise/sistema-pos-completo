{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://sistema-pos-completo.onrender.com/:path*"
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
