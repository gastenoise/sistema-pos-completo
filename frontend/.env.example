# Browser calls stay same-origin through /api and are proxied in local dev by Vite.
VITE_API_URL=/api

# Local Vite proxy target for backend API and /sanctum endpoints.
VITE_BACKEND_URL=http://localhost:8080
